# Git Bundle Local Config

> Local values for the cross-machine git bundle workflow demonstrated in
> `cross-machine-git-bundle.md`. General pattern is in that file;
> specific machine addresses, usernames, and passwords are in
> `astra-hub/references/user-stores.md` and the credential store.
