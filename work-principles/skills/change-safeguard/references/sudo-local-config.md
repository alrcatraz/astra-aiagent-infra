# Sudo Local Config

> Local values for the remote sudo workflow demonstrated in
> `remote-sudo-script-workaround.md`. General pattern is in that file;
> specific jump host details are in `astra-hub/references/user-stores.md`
> and the credential store.
