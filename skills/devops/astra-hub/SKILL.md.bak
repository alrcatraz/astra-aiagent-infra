---
name: astra-hub
description: "astar-* 生态地图索引：记忆/知识路由、项目索引、凭证安全指南、快速参考"
version: 1.1.0
author: alrcatraz
platforms: [linux]
---

# astra-hub — 生态地图索引

> 加载此 skill 后，我能获得 astra-* 生态的全貌导航。
> 配套 SOUL.md 5.1 使用。不存实际凭证值。

---

## 🗺️ 记忆与知识路由

| 查询对象 | 首选位置 | 备选 | 注意事项 |
|:--------|:--------|:-----|:---------|
| 用户偏好摘要 | `fact_store probe user_pref`（按 tag） | — | 新增偏好类别时更新 dynamic_ref "preference-query-map" |
| 完整参考文档 | `kb_search("dynamic_ref", 关键词)` | — | 每月 cron 检查更新 |
| 事故记录 | `kb_search("sre_incidents", 关键词)` | 本技能存储的经验 | 每次事故后写进去 |
| 服务配置 | `kb_search("hermes_config", 关键词)` | — | 部署即登记 |
| 服务状态 | `kb_search("service_mgmt", 关键词)` | — | cron 巡检更新 |
| 项目索引 | **本技能**（见下节） | — | 新增 astra-* 项目时更新这里 |
| 凭据查询 | **`credential-store-management` skill** | KeePassXC / GPG YAML / .env 三层 | 操作协议见该 skill |

---

## 🏗️ 公开/私有工作副本架构

> 所有 astra-* 项目遵循双重布局：`~/Projects/astra/<repo>` 存公开代码（推 GitHub），
> `~/.astra/repos/<repo>` 存私有工作副本（含个人数据覆盖层）。
> 此模式于 2026-06-17 建立。

### 目录职责

| 路径 | 内容 | 同步策略 |
|:----|:------|:---------|
| `~/Projects/astra/<repo>/` | 公开代码（已脱敏，无个人数据），remote → GitHub | 从 GitHub 推/拉代码 |
| `~/.astra/repos/<repo>/` | 私有副本（git clone 自 GitHub + 个人数据覆盖） | `git pull` 从 GitHub 同步代码框架 |
| `~/.astra/config/` | 个人配置文件（devices.yaml, format-convention.md） | .gitignored，不提交 |
| `~/.astra/scripts/` | 个人运维脚本（diagnose.py, diagnose.sh） | .gitignored，不提交 |

### 工作流

```
公开库改代码 → 推 GitHub:
  cd ~/Projects/astra/<repo> && git push

私有副本拉代码更新:
  cd ~/.astra/repos/<repo> && git pull
  个人数据文件（devices.yaml 等）已在 .gitignore，不受影响

私有副本添加个人数据:
  把 devices.yaml 放 ~/.astra/repos/<repo>/config/devices.yaml
  加到 .gitignore（须确认已存在）
```

### Fork 项目的 remote 约定

Camofox 来自上游项目 `jo-inc/camofox-browser`，fork 到 `alrcatraz/astra-camofox-browser`：

| 远程名 | 指向 | 用途 |
|:-------|:-----|:------|
| `origin` | `alrcatraz/astra-camofox-browser` | 你的 fork（可推送） |
| `upstream` | `jo-inc/camofox-browser` | 原项目（只读拉取） |

私有副本的 remote 命名用 `github` 而非 `origin`，避免与上游混淆。

## 📦 astra-* 项目索引

| 项目 | GitHub | 公开路径 | 私有副本 | 用途 | 关键文件 |
|:----|:------:|:---------|:--------|:-----|:---------|
| **astra-sre** | ✅ | ~/Projects/astra/astra-sre/ | ✅ ~/.astra/repos/astra-sre/ | SRE 协调整合层 | scripts/health-scan.py |
| **astra-knowledge-base-mcp** | ✅ | ~/Projects/astra/astra-knowledge-base-mcp/ | ✅ ~/.astra/repos/astra-knowledge-base-mcp/ | 知识库 MCP 服务 | server.py |
| **astra-camofox-browser** | ✅ fork | ~/Projects/astra/astra-camofox-browser/ | ✅ ~/.astra/repos/astra-camofox-browser/ | 浏览器自动化 (fork) | server.js |
| **astra-aiagent-infra** | ✅ | ~/Projects/astra/astra-aiagent-infra/ | ✅ ~/.astra/repos/astra-aiagent-infra/ | 门户 meta-repo + lifecycle-hooks | registry.yaml, lifecycle/astra-lifecycle-sync.py |
| **astra-aiagent-infra-template** | ✅ | — | — | 模板库 | — |
| **astra-vcs-assist** | ✅ | ~/Projects/astra/astra-vcs-assist/ | ✅ ~/.astra/repos/astra-vcs-assist/ | VCS 工作流编排（GPG 密钥、Git init、开发、发布、同步） | SKILL.md, routing.yaml, gpg/astra-vcs-assist-gpg-key/, git/skills/ |
| **astra-skill-execution-framework**
| **astra-skill-change-safeguard** | ❌ | ~/Projects/astra/astra-skill-change-safeguard/ | ✅ ~/.astra/repos/astra-skill-change-safeguard/ | 修改保全 checklists | SKILL.md |
| **astra-skill-deploy-register** | ❌ | ~/Projects/astra/astra-skill-deploy-register/ | ✅ ~/.astra/repos/astra-skill-deploy-register/ | 部署登记 checklists | SKILL.md |
| **astra-skill-pre-action-research** | ❌ | ~/Projects/astra/astra-skill-pre-action-research/ | ✅ ~/.astra/repos/astra-skill-pre-action-research/ | 执行前调研 | SKILL.md |
| **astra-skill-work-closure-check** | ❌ | ~/Projects/astra/astra-skill-work-closure-check/ | ✅ ~/.astra/repos/astra-skill-work-closure-check/ | 收尾闭环检查 | SKILL.md |
| **credential-store-management** | ❌（Layer 3） | 位于 astra-aiagent-infra 内 | ✅ `skills/devops/credential-store-management/` | 三层凭据管理协议 | SKILL.md, private/references/local-config.md |

### 已规划但未创建

| 项目 | 说明 |
|:----|:------|
| astra-sre-fix-synapse | Synapse 故障修复子 skill（规划中） |

## 🗂️ 非 Astra 项目归类

非 Astra 生态的项目移到独立类别目录：

| 类别 | 路径 | 项目 |
|:----|:-----|:-----|
| Linux 系统工具 | ~/Projects/linux/ | dnf-pri, rime-copr, nvidia-cuda-setup-* |

---

## 🔐 凭证安全访问指南

> 完整三层操作协议（KeePassXC / GPG YAML / .env）见 `credential-store-management` skill。

凭证管理分两个正交维度：

| 维度 | 按什么分 | 在哪看 |
|:----|:---------|:-------|
| **按归属**（个人/工作/其他/临时） | 谁拥有这台设备 | 本节下方表格 |
| **按存储工具**（KeePass / GPG / .env） | 什么类型的凭据 | `credential-store-management` |

### 凭证四组分类

| 分组 | 存储位置 | 适用范围 | 示例 |
|:----|:---------|:---------|:-----|
| **🔵 个人** | `~/Documents/credentials/personal-*.gpg` | 你自己的自有设备 | DS425Plus、SUSET01、VPS、NAS |
| **🟠 工作** | `~/Documents/credentials/work-*.gpg` | 工作相关的服务器和设备 | 客户服务器、BMC、项目环境 |
| **🟢 其他** | `~/Documents/credentials/other-*.gpg` | 朋友、家人、共享设备 | 同事机器、共享服务器 |
| **⚪ 临时** | 不持久化 | 一次性访问、临时协作 | 借调机器、临时远程协助 |

**使用方式：**
- 个人/工作/其他 → 按 `credential-store-management` 的三层协议执行
- 临时 → 用户当场告知，用完即弃，不落地

### 记忆里的凭证

完整规则见 `credential-store-management` → Credential Safety in Persistent Memory 章节。
核心原则：**memory / fact_store 中不允许存任何实际凭证值**，仅保留 `→GPG creds` 引用。

### 收尾闭环检查

任务收尾时的系统性检查清单（凭证残留、技能更新、记忆准确性、决策记录等）→
见 `work-closure-check` skill。配合 SOUL.md §4.3 使用。

---

## ⚡ 快速参考

### 自动维护机制一览

| Cron 任务 | 频率 | 类型 | 说明 |
|:----------|:----:|:----|:-----|
| **KeePass 同步** | 每 2 小时 | no_agent 脚本 | keepass-sync.sh — 从 NAS Sync 库 merge 进本地主库 |
| **E2EE 看门狗** | 每 1 分钟 | no_agent 脚本 | e2ee-watchdog.sh — 检测 E2EE 故障并触发修复 |
| **E2EE 健康检查** | 每 5 分钟 | no_agent 脚本 | e2ee-health.sh — Gateway 在线/响应验证 |
| **服务健康检查** | 每小时 | no_agent 脚本 | service-healthcheck-noagent.sh → healthcheck.py — 本地服务巡检 |
| **astra-sre 巡检** | 每日 08:00 | agent | health-scan.py — 全设备统一巡检（workdir: ~/.astra/repos/astra-sre） |
| **astra-sre 月维护** | 每月 1 日 09:00 | no_agent 脚本 | astra-sre-refresh.sh — 知识库刷新 + learn.py 扫描 |
| **skill-stale-check** | 每月 1 日 | no_agent 脚本 | skill-stale-check.sh — 检查技能是否有坑 |
| **会话清理提醒** | 每季度 | agent | 提醒清理旧会话 |
| **备份清理提醒** | 一次性 | agent | Synapse 升级一周后清理备份 |

### 后台服务一览

| 服务 | 类型 | 端口 | 启动方式 | 说明 |
|:----|:----|:----:|:---------|:------|
| **mihomo** | systemd | 7890/9090 | `sudo systemctl enable --now mihomo` | 代理引擎（headless），FlClash 替代品 |
| **Path Optimizer** | cron（规划中） | — | 脚本自动更新 /etc/hosts | 四路优选（EasyTier/Tailscale/ZT/公网） |

### 补丁管理

| 补丁 | 位置 | 用途 | 升级后恢复方式 |
|:----|:----|:-----|:-------------|
| mautrix-e2ee-reset-shared | ~/.hermes/patches/mautrix-e2ee-reset-shared.patch | Megolm shared 标记重置方法 | `cd venv/.../site-packages && patch -N -p1 < 补丁` |
| e2ee-upgrade package | ~/.hermes/patches/hermes-e2ee-upgrade-20260604.tar.gz | 旧 E2EE 恢复补丁包 | `tar xzf ... && git apply --check` |

### 关键维护脚本

| 脚本 | 用途 | 备注 |
|:-----|:------|:------|
| ~/.hermes/scripts/e2ee-watchdog.sh | E2EE 实时守护 (v5) | 配合 astra-sre-fix-e2ee skill |
| ~/.hermes/scripts/e2ee-health.sh | Gateway 健康检查 | 5分钟间隔 |
| ~/.hermes/scripts/e2ee-repair.py | 全自动 E2EE 修复 | 被 watchdog 调用 |
| ~/.hermes/scripts/e2ee-repair-keys.py | 重新共享密钥 (v3，保留旧会话) | 手动调用，不重启 Gateway |
| ~/.hermes/scripts/service-healthcheck.sh | 本地服务巡检包装器 | 每小时执行 |
| ~/.hermes/scripts/healthcheck-fallback.sh | 离线环境备用 | pypi 不可达时降级 |
| ~/.hermes/scripts/skill-stale-check.sh | 技能过时检查 | 月度检查 |

### 设备 SSH 别名

| 别名 | 目标 | 身份文件 |
|:----|:----|:--------|
| `ssh vps-hk` | root@10.20.4.10:2222 | ~/.ssh/id_ed25519 |
| `ssh vps-uk` | root@10.10.4.11 | ~/.ssh/id_ed25519 |
| `ssh ds425plus` | Alrcatraz@10.20.3.10 | ~/.ssh/id_ed25519 |
| `ssh suset01` | alrcatraz@10.20.2.14 | ~/.ssh/id_ed25519_jump_star |
| `ssh susetlearn00` | alrcatraz@192.168.0.20 | ~/.ssh/id_ed25519 |

### 知识库空间一览

| 空间 | 用途 | 谁维护 |
|:----|:-----|:-------|
| hermes_config | Hermes 配置：外挂服务/MCP/CLI/端口 | 部署时 |
| service_mgmt | 管理方案：健康检查/维护日志 | 运维时 |
| sre_incidents | 事故记录：根因分析/修复经验 | 事故后 |
| dynamic_ref | 会变的参考数据：Gateway长度/格式约定 | 月度 cron |

### SSHFS 远程开发挂载

当 VS Code Remote SSH 因 `agent host` bug（1.123.x futex 死锁）不可用时：
`sshfs homecentre01-easytier:/remote/path ~/Projects/homecentre01/`
详见 `references/sshfs-remote-setup.md`（含诊断方法、字体配置、sudo 绕过技巧）。

### 路径速查

| 路径 | 内容 |
|:----|:-----|
| ~/.hermes/SOUL.md | 工作原则（永久上下文） |
| ~/.hermes/config.yaml | Hermes 配置（模型/插件/技能） |
| ~/.hermes/.env | API key 和 token |
| ~/.ssh/config | SSH 别名配置 |
| ~/Projects/astra/ | 公开 astra-* 项目（推 GitHub） |
| ~/.astra/repos/ | 私有工作副本（含个人数据覆盖） |
| ~/.astra/config/ | 私人配置（devices.yaml 等） |
| ~/.astra/scripts/ | 私人脚本（diagnose.py 等） |
| ~/.astra/knowledge-base.db | 知识库 SQLite 数据库 |
| ~/Projects/linux/ | Linux 系统工具（dnf-pri, rime-copr 等） |
| ~/Documents/credentials/ | 凭证索引 + GPG 加密文件 |
| ~/Extra/DS425Plus/ | NAS 挂载的额外数据 |

### 新增组件提示

| 组件 | 类型 | 说明 |
|:----|:----|:------|
| **execution-framework** | skill (devops) | 任务分类路由框架，取代子 skill 的关键词触发。所有 astra 工作流 skill 的路由入口。 |
| **astra-lifecycle-sync** | 工具 (astra-aiagent-infra/lifecycle/) | 生命周期钩子同步工具。读取 `~/.astra/repos/astra-aiagent-infra/registry.yaml`，生成 closure/deploy 动态 checklist 到目标 SKILL.md。运行方式：`cd ~/.astra/repos/astra-aiagent-infra && uv run python3 lifecycle/astra-lifecycle-sync.py --update` |

## ⚠️ 坑与教训

### SOUL.md 编辑：保留上下文，只搬值

当需要将 SOUL.md 中的偏好外迁到 Fact Store 或 dynamic_ref 时：
- **保留**原则本身的解释性上下文（为什么这么选、适用的前提）
- **只搬走**具体的值/数字/偏好条目本身
- 外迁后在原位置留引用：`参见 Fact Store (tag: xxx)`

历史教训：2026-06-07 用户纠正"缩减掉了大量关键信息，不合适"——过度精简导致 SOUL.md 丢失了判断场景所需的上下文，用户不得不亲自重写完整版。(SOUL.md 5.1)
